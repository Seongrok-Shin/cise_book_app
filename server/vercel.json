{
  "version": 2,
  "rewrites": [
    {
      "source": "/api/(.*)",
      "destination": "/"
    }
  ],
  "routes": [{ "src": "/api/(.*)", "dest": "/" }],
  "builds": [{ "src": "*.js", "use": "@vercel/node" }]
}
